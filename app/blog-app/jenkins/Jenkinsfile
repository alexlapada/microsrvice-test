pipeline {
    agent any
    stages {
        stage('Build') {
            withMaven(traceability: true){}
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
        stage('Test') {
            withMaven(traceability: true){}
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
    }
}